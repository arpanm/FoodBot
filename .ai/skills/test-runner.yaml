name: TestRunner
description: Execute automated tests and return structured telemetry

inputs:

- test_suite

outputs:

- test_report

tools_required:

- test_runner

rules:

- Must not modify code
- Must produce machine-readable output
