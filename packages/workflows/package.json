{
  "name": "@foodbot/workflows",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "description": "Temporal workflow definitions, activities, and workers for FoodBot",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "lint": "eslint src --ext .ts",
    "lint:fix": "eslint src --ext .ts --fix",
    "worker": "ts-node src/workers/worker.ts",
    "worker:manager": "ts-node src/workers/worker-manager.ts"
  },
  "dependencies": {
    "@temporalio/activity": "^1.11.3",
    "@temporalio/workflow": "^1.11.3",
    "@temporalio/client": "^1.11.3",
    "@temporalio/worker": "^1.11.3"
  },
  "devDependencies": {
    "@temporalio/testing": "^1.11.3",
    "@types/jest": "^29.5.14",
    "@types/node": "^22.10.2",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.2"
  }
}
