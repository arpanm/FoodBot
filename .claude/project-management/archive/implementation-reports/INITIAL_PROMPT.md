high level requirement prompt is as follows, it should have claude multi agent development environment with required sequential and parallel tasks to expand the requirement, plan the tasks, plan the development, generate test cases, generate code, run tests and fix issues, do code review, do code analysis, do security audit, fix all code, test, security issues:

Claude code to do a complete spec based development with capacitor+react frontend and node+nest backend with Claude , OpenAI and gemini llm model api usage in respective areas with on/off configuration and api keys. Ui should have chatbot experience but with different options showing cards with image+text+attributes, also different cta buttons and different input fields or options showing in the chat with rich ui elements/components. Chat prompt should be passed along with user id to backend. It should create a async job and return jobid. Backend will enrich it with customer personalization data from redis/graphdb (user context preference graph should be stored as tree like day of week - hour of day - category - subcategory - restaurants - dishes etc). Also prompt to intent/workflows should be cached in vector db to reduce llm api calls or saving tokens. Llm should return intent and workflow json. Then this workflow should be saved in temporal db. And temporal should execute the workflow with all resiliency like error handling, retry, circuit breaking, bulk head, back tracing, alternative plan execution etc. Each steps of llm, temporal execution steps etc should update statuses against the jobid. Frontend will keep on polling the jobid till fully done and keep on showing status updates in proper user specific messages. Temporal node will call MCP server APIs . Have mock MCP APIs ( we will implement a restaurant app and backend properly as restaurant agent with restaurant onboarding, restaurant menu management, restaurant order management and restaurant analytics layer, this MCP layer will aggregate all restaurants agents and orchestrated properly between customer agent and restaurant agent, we will need a elastic search layer inbetween to have all restaurants, dishes, price and availability data added & indexed to it with search, filter etc APIs and kafka based updates for indexing, restaurant agent frontend app should be capacitor+ react. Backend should be node+nest, inbetween elastic search management and orchestration layer should be of java spring boot. Restaurant owners can use the agent to do any analytics or order or menu or insight level queries, also see list of orders and update any order status, also see menu listvand add/edit menu also mark any items available/not available) like search restaurants by name or type or dish bame, search dish by name or type or restaurant name, get filters details against search, apply filters against search, get restaurant details with all dishes with price and availability, get dish details with price and availability, recommendate dishes, add to cart, get cart details, get list of address, add orvedit address, select address against cart, checkout, payment initiate with payment option, update payment status, get payment status with order details, get order details, get list of order, get order tracking details, cancel order, give feedback against order or restaurant or dish etc etc. it should also have a layer implementing and calling Swiggy mcp server: https://github.com/Swiggy/swiggy-mcp-server-manifest . Also one more for Zomato MCP server: https://github.com/Zomato/mcp-server-manifest . It should have configuration to enable /disable Swiggy, zomato and mock MCP layer, so that temporal layer call Claude should create requirements details, break requirements to tasks, generate test cases, generate code with all gurdeails, execute test cases, do code review, do static & dynamic code analysis, do security analysis, fix code review issues, fix test bugs, fix code quality issues, fix security issues etc.